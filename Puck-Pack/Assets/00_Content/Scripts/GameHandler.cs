using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameHandler : MonoBehaviour {

	[SerializeField] private UIHandler uiHandler;

	//So, this needs to activate the right things when events happens then?
	//Yeah, so, when UI handler says: This button was pressed, then this should start BuuMan then?
	//Meaning activating a bunch of things yeah?
	//Probably better then to have a BuuManHandler? Nah, let's have this one do that surely? //Well no, it should be better if BuuMan can do all of that on it's own...



	void Start() {

	}

	void Update() {

	}
}
